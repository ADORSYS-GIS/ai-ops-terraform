#nodeSelector:
#  karpenter.sh/controller: 'true'
dnsPolicy: Default
settings:
  clusterName: ${clusterName}
  clusterEndpoint: ${clusterEndpoint}
  interruptionQueue: ${interruptionQueue}
  defaultInstanceProfile: ${instanceProfile}
%{ if customAmiId != "" }
  defaultAMI: ${customAmiId}
%{ endif }
webhook:
  enabled: false